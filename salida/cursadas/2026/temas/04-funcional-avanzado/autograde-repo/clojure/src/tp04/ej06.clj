(ns tp04.ej06)

;; Ejercicio 6 — Colecciones persistentes
;; Trazabilidad: F-13

(defn agregar-al-vector
  "Retorna un nuevo vector con elem agregado al final.
   El vector original no se modifica."
  [v elem]
  ;; TODO: Implementar
  )

(defn actualizar-mapa
  "Retorna un nuevo mapa con la clave k asociada al valor v.
   El mapa original no se modifica."
  [m k v]
  ;; TODO: Implementar
  )

(defn combinar-mapas
  "Retorna un nuevo mapa con todas las claves de m1 y m2.
   En caso de conflicto, m2 prevalece."
  [m1 m2]
  ;; TODO: Implementar
  )
