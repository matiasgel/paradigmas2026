# EDU Standalone — Dependencias Python
# ======================================
# Instalación rápida desde la raíz del módulo:
#   pip install -r requirements.txt
#
# Con entorno virtual (recomendado):
#   bash scripts/setup.sh          ← crea y configura .venv automáticamente
#   source .venv/bin/activate      ← Linux/macOS
#   .venv\Scripts\activate         ← Windows
#   pip install -r requirements.txt

# Google APIs (Slides + Drive + OAuth)
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1

# Config y serialización
pyyaml>=6.0.1

# HTTP para Gemini REST
requests>=2.31.0

# Renderizado de tablas como imágenes
matplotlib>=3.7.0
Pillow>=10.0.0

# Exportación PDF (pandoc incluido via pypandoc_binary)
pypandoc>=1.13
pypandoc_binary>=1.13
